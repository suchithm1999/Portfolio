import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'suchith.m',
  appName: 'suchith-m',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
